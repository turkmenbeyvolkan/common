{
  "name": "@tv_tickets/common",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "types": "build/",
  "scripts": {
    "clean":"del-cli ./build/*",
    "build":"npm run clean && tsc",
    "pub":"npm run build && git init && git add ."
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.6.2"
  }
}
